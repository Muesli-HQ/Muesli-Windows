Place human-recorded WAV files here. Filenames must match the manifest `audio`
paths (for example `short-command.wav`).

Do not check in dummy WAVs labelled as human-reviewed. `*.wav` is gitignored.
A human must record or select each file and listen before writing the matching
reference.
