Write one `.txt` file per transcript case after listening to that WAV.
Silence and background-noise cases must not get a text reference.

Do not copy model output into these files and label them reviewed. Empty
files and model-only provenance are rejected by
`scripts/test-transcription-corpus.ps1`.
