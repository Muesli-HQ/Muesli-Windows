Place human-written `.txt` references here after listening to the matching WAV.

Do not paste model output into these files and label them reviewed. Leave this
directory empty until a human writes each reference.
