<Window x:Class="Muesli.Windows.MeetingLiveTranscriptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Live meeting transcript" Width="380" Height="340"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="CanResizeWithGrip"
        ShowActivated="False" MouseEnter="Window_MouseEnter" MouseLeave="Window_MouseLeave">
  <Border CornerRadius="12" Background="{DynamicResource BackgroundRaisedBrush}" BorderBrush="{DynamicResource BorderBrushMedium}" BorderThickness="1" Padding="14">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <Grid MouseLeftButtonDown="Header_MouseLeftButtonDown">
        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <StackPanel>
          <TextBlock Text="Live transcript" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" />
          <TextBlock x:Name="StatusText" Text="Waiting for speech…" Margin="0,3,0,0" FontSize="11" Foreground="{DynamicResource TextTertiaryBrush}" />
        </StackPanel>
        <Button Grid.Column="1" Content="Copy" Margin="8,0,0,0" Padding="8,3" Click="Copy_Click" Style="{StaticResource GhostButton}" />
        <Button Grid.Column="2" Content="×" Margin="6,0,0,0" Padding="8,3" Click="Dismiss_Click" Style="{StaticResource GhostButton}" />
      </Grid>
      <Grid x:Name="WaveformPanel" Grid.Row="1" Margin="0,12,0,2" Visibility="Collapsed">
        <Grid.ColumnDefinitions><ColumnDefinition Width="48"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
        <Grid.RowDefinitions><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
        <TextBlock Text="You" FontSize="10" Foreground="{DynamicResource TextTertiaryBrush}" />
        <ProgressBar x:Name="MicLevel" Grid.Column="1" Height="5" Maximum="1" />
        <TextBlock Grid.Row="1" Text="Others" Margin="0,5,0,0" FontSize="10" Foreground="{DynamicResource TextTertiaryBrush}" />
        <ProgressBar x:Name="SystemLevel" Grid.Row="1" Grid.Column="1" Margin="0,5,0,0" Height="5" Maximum="1" />
      </Grid>
      <ScrollViewer x:Name="TranscriptScroll" Grid.Row="2" Margin="0,12,0,0" VerticalScrollBarVisibility="Auto">
        <StackPanel>
          <TextBlock x:Name="CommittedText" Text="" FontSize="13" Foreground="{DynamicResource TextPrimaryBrush}" TextWrapping="Wrap" />
          <Border x:Name="OthersPartialBorder" Margin="0,8,38,0" Padding="8" CornerRadius="7" BorderThickness="1" BorderBrush="{DynamicResource BorderBrushSoft}" Visibility="Collapsed">
            <TextBlock x:Name="OthersPartialText" FontSize="12" FontStyle="Italic" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" />
          </Border>
          <Border x:Name="YouPartialBorder" Margin="38,8,0,0" Padding="8" CornerRadius="7" BorderThickness="1" BorderBrush="{DynamicResource AccentBlueBrush}" Visibility="Collapsed">
            <TextBlock x:Name="YouPartialText" FontSize="12" FontStyle="Italic" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" />
          </Border>
        </StackPanel>
      </ScrollViewer>
    </Grid>
  </Border>
</Window>
